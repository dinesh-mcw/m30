"""
Simple boilerplate setup file so that legacy ``setuptools`` installations
can access the ``setup.cfg`` file without requiring explicit support.
"""

from setuptools import setup


if __name__ == '__main__':
    setup()
